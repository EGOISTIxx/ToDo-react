import { Form } from 'antd'
import styled from 'styled-components'

export const StyledForm = styled(Form)``

export const StyledFormItem = styled(Form.Item)``

export const StyledTodoHeading = styled.h1``
